[project]
name = "training-new-gridfm"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy", "pyyaml", "torch", "torch-geometric"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100

