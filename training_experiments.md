# Training experiment ledger

One line of config-delta and one of verdict per experiment. Full forensic detail stays in
`experiments.md` (§25 for the 2026-07-17 decoder/plateau work); this file is the quick map.
Conventions: v_skill = unseen |err|/|dv| (1.0 = no better than predicting dv=0);
gate config = 60 feeders balanced over all 4 corpora, small-first, bs8, spe1600, 40 ep,
lr 4e-4 anneal, norm-loss, residual gauge, task=random, exact decoder.

| id | jobs | delta vs gate config | result | verdict |
|---|---|---|---|---|
| T01 seed tests | 15237232-34 | PRE-fix: 40f SMART-DS only, mixed loss, no gauge/anneal, 8 ep | unseen v_skill ~1.0 all seeds, train V > null | FAIL — known-bad loss (no norm) + ~10x too short |
| T02 overfit sweep | 15240844 | ONE fixed batch (6 SMART-DS feeders), 400 Adam steps/config | FINAL@400: mixed 0.293, norm 0.422, **vonly 0.236** (+ best i_wape 1.25%, NO current loss), vonly_hi 0.389 | optimization works; decoder-pullback theory REFUTED; horizon was the killer |
| T03 gates x3 | 15241690-92 | gate config, seeds 0/1/2 | train 0.60/0.71/0.76, unseen 0.92/0.90/0.96 flat; kcl->2e-6, I~2% | NO LAUNCH: learns but does not transfer at 60f — generalization gap, not optimization |
| T04 abl pf | 15241727 | task=pf | train 0.437, unseen **0.816** (best) | matched conditional wins at small scale; random needs topology diversity to pay off |
| T05 abl lr | 15241728 | lr=1e-3 | unseen 0.907 | no gain over 4e-4 |
| T06 abl steps | 15241729 | steps=24 | unseen 0.914 FINAL (ep40) | no effect either way; BOTH mid-run reads (0.905@10, 1.092@28) were noise — only finals count |
| T07 abl width | 15241730 | hidden=384 | unseen 0.910 | no capacity win at this scale |
| T08 abl nonorm | 15242531 | mixed loss (NORM=0) | unseen 0.905 FINAL | norm vs mixed indistinguishable in generalization |
| T09 ddp smoke | 15241743 | 2-rank torchrun, tiny run | QUEUED (QOS) | validates the 4-GPU full path |
| T10 scale gate | 15248741-43 (v4) | feeders 60->240 (random s0/s1 + pf control), 30 ep | RUNNING | THE decision: unseen skill responds to feeder count -> full launch; flat -> model work first. v1-v3 died at startup: IEEE30 refusal, bridge-chord collate refusal, 5% gate on a 30-feeder eval split -- all three now handled (loud-skip; train-only gate) |
| T10 FINAL | 15248741-43 | -- | random s0 0.92->**0.824**, pf 0.816->**0.765**, random s1 train-stalled 0.85 -> unseen 1.049 | SCALE WORKS (2/3); s1 = optimization wedge, suspect = ic_head sinh explosion (ic_wape 16713% in ddp smoke) -> clamped z to +-8 |
| T11 vonly | 15254796 | 240f, W_I=0 W_KCL=0 | FINAL unseen **0.789** (train 0.573) -- BEST of campaign | Confirms T02 at scale: exact decoder couples I to V, so explicit I loss is redundant AND injects stiff reactor/xfmr gradients (reactor 8.7e14%, xfmr 2666%) that fight V. Drop I loss; currents stay machine-exact from decoder. THE full-launch config |
| T12 stability | 15254801 (s2) | 240f s2: ic-clamp + warmup 0.1 | FINAL unseen **0.848** COMPLETED -- looked wedged at 0.98 mid-run, converged late | WEDGE FIXED (clamp+warmup); mid-run reads deceive again. s1 (15254800/15255130) died 3x on shm crashes -- see shm note below. Gate evidence: s0 0.824 / vonly 0.789 / s2 0.848, three clean runs within 0.06 -> FULL LAUNCH JUSTIFIED |
| T13 fix matrix | 15254842-44, 15255068 | 240f seed0, one var each: PE hopcap 150 / lr 2e-4 / bs16 / fb-points 3 | FINALS: pe150 1.182 FAIL (train 0.554 = memorizes, no transfer); **lr2e4 0.789 TIES vonly** (late anneal 0.833->0.789); bs16 1.308 FAIL; fb3 died ep13 (1.103, failing) | TWO winners at 0.789: vonly@4e-4 and I-loss-on@2e-4. Lower peak LR is a real generalization knob |
| T14 FULL LAUNCH | 15256412 (s0), 15256576 (s1) | 4xH100 DDP, ALL feeders, task=random, vonly (W_I=0). s0=lr4e-4 120ep/40h; s1=lr2e-4 48ep/16h backfill (combo bet from lr2e4 tie; replaced 15256442) | s0 DIED end of ep1 (NCCL watchdog): rank 0 evals 319 unseen feeders x 10 variants x 3 lenses ALONE (~9.4k forwards) while ranks 1-3 enter ep2 and their allreduce times out at 10 min. Probe-scale evals were small, so the bug only bites at full scale. s1 killed before hitting the same wall | FIX: --eval-feeders cap 48 + 4h pg timeout (commit b21b51d). RELAUNCHED as 15259366/67, then AGAIN as **15261952 (s0, 32ep/40h) / 15261953 (s1, 12ep/16h lr2e-4)**: measured 3552s/epoch at full scale, so 120ep/48ep could never finish -- the cosine anneal (where T13 showed the gains land) must fit the walltime |
| T16 pf-first probes | 15259369-72 (minimal), 15259373-74 (240f steps), 15259375->15259503 (e2e) | USER DIRECTION: random masking stays THE objective, but nail the pf mask first (V hidden, Icomp+Y+slack visible = OpenDSS's own linear problem: Ysystem + Norton Icomp, one solve -- manual confirms). Curriculum: minimal_component alone x {base, --no-feat (no asinh), --vabs (absolute V head), both}; multi-corpus 240f steps 12 vs 32 (does pf need MP depth?); e2e = fix_lr2e4 Icomp estimates -> direct fp64 solve vs its V head | minimal FINALS (unseen pf skill): base 0.407, nofeat 0.397, vabs 0.339, **vabs+nofeat 0.240** (train 0.087; its c10 abort was the nonfatal teardown race -- all 40 eps ran). pf240_s12 FINAL 0.770 (matches T10 0.765) | **USER'S BOTH HYPOTHESES WIN AND COMPOUND on minimal: -41% vs baseline.** asinh alone a wash but interacts with vabs. inj ic_wape 100% expected: pf mask never hides ic, so ic_head is untrained in pf-only probes. Migration probes launched: pf240_vabs 15259783, pf240_vn 15259784, rnd240_vn 15259785 (+15259782 vn replicate on minimal) |
| T18 migration + steps + ic | 15259782-85, 15259374, 15259636-37 | vabs/nofeat to multi-corpus (pf + random); steps 12 vs 32; w_ic-focused training | pf240_vabs 0.910, pf240_vn 0.900 vs s12 base **0.770**; rnd240_vn 1.062 vs vonly 0.789; s32 0.850; mc replicate 0.326 (vs 0.240 -- run variance). ic_wape finals: ic_only 107.2, ic_heavy 102.2, vonly base 107.7, fix_lr2e4 156.0 | **vabs+nofeat is a MINIMAL-ONLY win -- does NOT transfer** (SMART-DS dv ~4% makes residual frame a strong prior; minimal dv 36.6% favors absolute). Full-run config (dv-mode vonly) stands. More MP depth loses at probe budget. ic quality saturates ~100-107% WAPE under every loss recipe -> estimator needs a mechanism (physics/iteration), not weights. e2e on ic ckpts: 15260346/47 |
| T19 JOINT SOLVE | 15260346/47 (v1), 15260824 (two-stage) | random mask: augmented LINEAR system [V_hidden, dIcomp_hidden] using the mask's visible V measurements (KCL rows at all non-source nodes), min-norm around the model estimate | v1: **machine precision 1e-12..1e-8 on ~half the random samples incl. 23-40% hidden ic** (determinate: measurements+KCL pin everything); underdetermined corners BROKE (11.9 vs plain 0.044) because np.lstsq min-norms V too | the random-mask task is largely CLOSED by linear algebra where determinate; NN value concentrates on underdetermined corners + future Y estimation. ic ckpt choice immaterial (ic_only == fix_lr2e4 solve cols): estimator saturation confirmed. v2 = two-stage min-||delta|| (delta=0 feasible -> joint >= plain everywhere) |
| T19c joint v3 WORKS | 15261204 | v3 = rcond=1e-8 truncation + instability fallback (delta=0) | random lens: **20/22 samples <= 2.4e-5 or plain-level** (reactor 981 -> 2.0e-4; case3 0.033 beats plain 0.044; determinate stay 1e-12..1e-8). Stragglers: t1_1250 xfmr 0.175, t1_1267 0.006 (joint > plain there) | THE inference recipe: model estimate -> truncated joint projection; add plain-vs-joint model selection by OBSERVABLE residual (measurement/KCL) to close the 2 stragglers. vs best-ever pure-NN 0.789: 4-11 orders better |
| T20 SCORECARD | 15261987 | 43 unseen feeders x {pf, random}, ic_only ckpt through the physics stack | **pf: solve med 4.1e-9 / worst 1.2e-6 (machine precision corpus-wide, 0 learned params). random: joint med 3.3e-8, plain med 2.1e-3 / worst 4.4e-2; joint worst 0.86 = the stragglers.** head med 1.000 on both | END-OF-DAY NUMBERS. vs pure-NN best-ever 0.789: median improvement ~7 orders (random) / ~8 (pf). Remaining: plain-vs-joint selection by observable residual (kills joint worst-case); underdetermined corners = the model's estate; Y estimation next capability |
| T21 LEAKAGE AUDIT | 15263067 | external-review falsification battery: leak-proof rhs (NaN sentinel, mask from batch), nullity(A)/sample, zero-prior joint0, visible-V sweep sw80/50/20/0 | **NO LEAKAGE: sentinel never tripped, results survive** (random joint med 1.67e-8 vs 3.3e-8 pre-audit). nullity-0 samples: 2.07e-12. Sweep degrades EXACTLY as identifiability predicts: sw80 8.0e-8 -> sw50 9.4e-6 -> sw20 3.2e-4 -> sw0 28.3. **joint0 == joint everywhere: the current NN adds ~ZERO over a zero prior** | Attribution settled: performance is physics (determinacy + V-through-node-sums: two hidden ics at one node leave only their sum determined, and V only depends on the sum -- so V stays exact at nullity>0). Caveats: nullity tol (1e-8*|Y|max) overcounts on stiff Y (pf shows nullity>0 rows at 2.4e-8); sw50/20 maxes (299/1.3e5) = truncation instability -> plain-vs-joint selection needed; sw0 joint (28) loses to plain (0.05) -- no measurements = plain's regime. THE HONEST GAP: make the NN prior actually worth something (mechanism for ic; Y capability) |
| T22 Y-recovery: EXCITATION | 15263085 | user scenario: line/xfmr Y hidden, recover from K=1..8 snapshots of truth (V, Icomp) via KCL+lstsq | **FAILS BY ALGEBRA: Y_relerr ~1.0 or explodes (1e5-1e10) regardless of K; downstream V often stays exact anyway** (minimal xfmr: Y 100% wrong, held-out V 1.6e-8) | PERSISTENT-EXCITATION LESSON: variants = load rescalings -> stacked V nearly collinear -> snapshots add rows not rank. Unidentified Y directions = the ones the operating distribution never excites (so V fine in-dist, breaks OOD). **Parameter estimation NEEDS the learned prior (conductor family/structure); algebra pins only the excited scale.** Same two-stage projection as T19 with NN prior as anchor. This is the measured case for the foundation model |

## The objective (user, 2026-07-17)
The reduced format makes the ENTIRE grid four arrays -- V, I_bus, Icomp, Y -- and every
task is a visibility pattern over them (pf/se/injection/param are 4 examples of many;
there is no P/Q or anything else in this representation). THE objective: mask any subset
of the four, reconstruct the rest -- under bad/noisy visible entries too.
Status per array: V maskable (done), Icomp maskable (done), I_bus target-only (must
become a maskable INPUT; its rows are linear equations), Y always-visible (must become
maskable + Y head; excitation-limited per T22, so the learned prior carries it).
| T23 SPEED 3.0x | 15263308/09 (A/B), 15263314/15 (prof), 15263403 (merged) | training-efficiency campaign (USER: faster code = more experiments) | 98s/ep -> 47s (--no-cur: skip recon-ctx + current decode the vonly/4-array losses never read; loss identical) -> 33s (merged-edge MP: one MLP call/store/step, 3-step trajectory BIT-IDENTICAL). PROF split after: data 3% h2d 2% fwd 45% bwd 50%. REJECTED: bf16 (56s, cast overhead > matmul savings at these sizes), torch.compile (hetero-shape recompile storm, fwd 36->77s) | probes now ~22 min/40ep. Next levers if needed: block-diag grouped edge-MLP across stores, bigger BS. --eval-every N available for long runs |
| T25 T6-CTX REPLICATED | 15264921-25 | ctx2 vs ctx0 (random4) on two random subsets + inj_ctx2; class-conditional metrics live | **ctx WINS on V, replicated: subset-a unseen 2.13->0.94, subset-b 1.73->0.99; class D (joint completion) 3.7-4.2 -> 1.18-1.20 (3.5x)**. REPLICATED NEGATIVE: class-B ic stays broken with or without ctx (789-1215%, worse than zeros) and inj_ctx2 148-157% -- other operating points teach response SHAPE but the target variant's load AMPLITUDE is exactly what is hidden. y_head still diverges at generalization scale (par 4.8M-15M%) though the micro-overfit proved the codec sound (1.3% in 200 steps incl. stiff lines) | CONFOUND FOUND+FIXED: subset shuffle broke per-corpus balance (~63% minimal by count) -> domain shift; stratified per-corpus shuffle committed. Within-pair comparisons valid. NEXT: stratified re-test; B-class ic supervision is actively harmful -> gate it out or leave B to physics (inj lens is algebraically exact through the joint solve anyway); ic/y heads' estate = class D |
| T25b stratified re-test | 15265243/44 | ctx pair on BALANCED random subset (stratified shuffle) | sctx0 1.614 -> sctx2 1.141; class D 2.68 -> 1.42. **THIRD ctx replication.** Both configs UNDERFIT (train ~1.0): random-composition 240f/40ep cannot even fit big-feeder training data -- the small-first-vs-random gap is CAPACITY/STEPS on large feeders (consistent with T10 scale-works; s1 full-corpus fits 0.64 by ep4) | ctx = adopted for the four-array config. Probe protocol note: small-first probes overfit small nets, random-subset probes underfit -- read both, trust full-scale |
| T26 d-only + MESH | 15266000/01 | vs sctx2 baseline (1.141 / D-ic 184.3 / B-ic 593): (a) --ic-d-only drops class-B ic supervision; (b) --with-mesh admits meshed feeders (tree decoder skipped under --no-cur) | d-only: **ic improves BOTH classes (D 184->129%, B 593->304%)** + fits far better (train 0.63 vs 0.97) but unseen V 1.25 vs 1.14 (needs a replicate). mesh: **ZERO exclusions (240/240 + 30/30), stable training, build 2560s->26.8s (~100x)**; unseen 0.995 on the harder mesh-inclusive eval (null 17.8%), C=0.886 | the 'unseen = unseen RADIAL' qualification is DEAD under --with-mesh; d-only adopted for the ic path pending a V replicate; SCE probe (15266078) stacks next |
| T26b SCE | 15266078 | GraphMAE scaled-cosine-error as the ic loss (stacked on d-only+ctx) | **V ties best (1.144; class-D V 1.084 = best at probe scale) but ic explodes (B 982k%, D 170k%)**: cosine supervises DIRECTION only; magnitudes free-float, and our estimates feed a physics solve in real units | SCE-as-sole-loss REJECTED; hybrid (SCE + light magnitude anchor) is the follow-up. Consistent pattern across wy0/d-only/SCE: the less the ic objective constrains the trunk, the better V -- gradient conflict is the V bottleneck at probe scale |
| T26 MESH CLOSED | 15266023/24 (train), 15266155 (e2e) | --ic-d-only + --with-mesh exercised; e2e solve on previously-excluded meshed feeders | **refined (ctx+ic-d-only): unseen 0.938, class-D ic 184->134% (same subset as sctx2 1.141 -- stacking win). MESH: 37Bus pf solve EXACT, random joint 2.18e-13 at 53% hidden ic (best random result of the campaign, ON A MESHED FEEDER); IEEE9500 (9550 nodes, 6 bridge chords) pf solve EXACT** | mesh capability CLOSED by physics: radial/sparse-mesh/heavy-mesh/10k nodes -- one fp64 solve, zero learned params. Mesh training runs mechanically (rnd4_mesh; eval null changes to 17.8% -- mesh-inclusive baselines needed for comparisons). ctx+ic-d-only = the four-array config of record |
| T26c HYBRID-SCE WINS | 15266159 | direction-SCE + 0.1 magnitude anchor (stacked on d-only+ctx, subset 301) | **unseen 0.783 -- best random4 of campaign on the HARD subset protocol** (chain: base 1.141 -> d-only 1.252 -> pure SCE 1.144 -> hybrid 0.783); class C 0.789, **class D 0.995 = first sub-1.0 D**; train 0.477. ic mags still off (D 3180%, 50x better than pure SCE) -- anchor 0.1 too weak | REPLICATE on subset 302 + anchor 0.5 variant before adoption (protocol). Direction-first ic supervision appears to be the conflict-free form the trunk wanted |
| T27 HYBRID REPLICATED | 15270298/99 | protocol replicate (subset 302) + SCE_MAG 0.5 variant | **REPLICATES: 0.710 on subset 302 (vs 0.783 on 301); class D sub-1.0 on BOTH (0.811/0.995)**. Anchor 0.5: V 0.815 (small cost) but ic D 3180->968% (3.3x) | **CONFIG OF RECORD CONFIRMED: --no-cur --ctx-points 2 --ic-d-only --ic-sce** (SCE_MAG 0.1 best-V / 0.5 balanced -- pick 0.5: estimates feed the solve). Assembled entirely from replicated probe wins. Next full-scale run trains THIS config on the mesh-inclusive corpus |
| T28 s1 FULL-SCALE FINAL | 15261953 | 12ep lr2e-4 full corpus (old two-array config), full anneal | **unseen 0.329 (train 0.206) -- 2.4x the campaign's previous best; se lens 0.231; inj ic_wape 68.25% -- THE 100% SATURATION FLOOR BREAKS AT SCALE** (probe-scale 'estimator adds zero' was a scale artifact) | scorecard e2e on its ckpt queued: with estimates beating the zero prior, T21's joint0==joint verdict should flip -> first measured MODEL VALUE through the solve. And this is the OLD config -- the replicated T27 config + mesh corpus awaits sign-off for the next full run |
| T28b JOINT BEATS JOINT0 | 15271211 | e2e scorecard on s1's ckpt (runs/dk_foundation_15261953/last.pt), 48 unseen feeders, random+pf lenses | **FIRST MEASURED MODEL VALUE THROUGH THE SOLVE: random-lens underdetermined feeders (nullity>0, 27/37) joint med 8.02e-07 vs joint0 (zero-prior) med 5.35e-06 = 6.7x; worst case also better (joint max 0.61 vs joint0 1.37). T21's joint0==joint verdict is FLIPPED.** Determinate feeders machine-exact as always (nullity-0 joint med 2.0e-13); pf lens exact and prior-independent (8.56e-10 both, expected: ic visible => nothing to estimate) | The full chain now works end-to-end: scale-trained estimator (inj ic_wape 68.25%) -> joint projection -> better V exactly where algebra alone cannot decide. Model value lives ONLY in the nullspace, as the architecture review predicted. Next lever: T27 config (+ctx, +ic-sce) should improve the estimates feeding this same solve -- awaiting user sign-off for the full-scale run |
| T17b pf lens exact | 15259626 | multi-lens e2e: pf mask (truth Icomp) through the FULL data pipeline -> solve | skill_solve 0.000 on ALL feeders incl. 10471-node SFO P13U (head: 0.21-1.96). Only skips = 37Bus/IEEE9500, blocked by the TREE DECODER's radial requirement, not the solve | pf capability = machine-exact, zero learned params, corpus-wide. Mesh feeders are decoder-blocked only -> no-recon mode (task #9) unlocks them |
| T24 PE + Y-head arc | 15263881/82 (PE), 15263355-460/786-788 (Y v2-v4 + wy0) | PE ablation pair on the 0.789-class config; Y-codec iterations | **PE ESSENTIAL: no-PE train 1.012 / unseen 1.284 (cannot even fit train) vs baseline 0.583/0.831; with T13 pe150 (memorizes, 1.182) the dose-response is complete -- current PE = sweet spot.** rnd_basef 0.831 revalidates the fast path on the real objective. Y-head: v2 feat-space loss, v3 per-position scales, v4 spike-and-slab gate all FAIL (par y_wape 388k-3.2M%); **--w-y 0 recovers trunk to 0.888 -> Y gradients were poisoning the shared trunk -> y_head now DETACHED** | Y estimation remains unsolved as a learned head; next: micro-overfit store breakdown -> structured family-parameter codec. Trunk safe via detach either way |
| T17 E2E SOLVE WINS | 15259503 | fix_lr2e4 ckpt, random mask, 15 unseen feeders: hidden-Icomp ESTIMATES + truth visible -> one fp64 Ybus solve, vs the same ckpt's V head | **skill_solve 0.000-0.043 vs skill_head 0.68-2.05 -- ~100x, on EVERY feeder**, even at 49-67% hidden Icomp (IEEE123 0.010 vs 0.675; 1638-node SFO P6U 0.008 vs 2.045; 5037-node P9U 0.004 vs 0.756) | **ARCHITECTURE DECIDED (measured): the GNN's V head is dead weight. Model = hidden-Icomp estimator; V = direct fp64 solve of Ybus (topology-agnostic: mesh or radial). pf lens exact by construction; random/se lens ~0.01 TODAY with an ic_head that was never the training focus. Next: solve-decoder eval path + pivot training weight to ic quality** |
| T15 solver probes | 15256418/491/636/688 (v1-v3+diag) | ladder vs DIRECT fp64 solve of full Ybus, truth Icomp, 5 feeders x 4 corpora | **DIRECT SOLVE: 20/20 machine precision, worst 4.4e-8** (yd-xfmr that DIVERGED the ladder: 1.6e-11). Ladder 18/20; its one divergence = As singular in delta zero-seq, grounded only by shunt load Y (diag) | **SOLVER LAYER = ONE SPARSE fp64 SOLVE of Ybus.** cond is ~4e9 here, not the old 1e18 -- no iteration, no splitting, no xfmr null-space needed for V. P10U gain ~500 persists through direct = REAL physical stiffness of zero-seq injection modes (problem property, 5 orders below V->I 2e7). Design: model estimates hidden Icomp (directly supervised); V = solve(Ybus, Icomp). pf lens becomes EXACT by construction. Next: e2e = fix_lr2e4 ckpt (trained ic_head, 0.789) ic estimates -> direct solve V vs its V head |

**shm crash note**: 'could not unlink shared memory file' aborts (s1 x3, fb3) happened MID-RUN,
not at teardown, and both victims shared a node with another of our jobs -- file_system sharing
+ co-tenant /dev/shm is the correlate. Full run holds its node alone (4/4 GPUs) so exposure is low.
Durable fix if it recurs: consolidate batched recon-ctx into a few large tensors.

## Standing decisions (why the gate config looks like this)
- **exact decoder in-model** (6.7e-01 -> 3.4e-06 on truth V; vsource was silently zero before)
- **norm-loss default**: without it the current term drowned V (v_skill ~1.0, measured twice)
- **residual gauge + anneal**: the reference PINN's 7.5e-08 recipe (t5_kcl_fp64_gauge)
- **task=random**: one random conditional per sample over V+Icomp; pf/se/injection are
  inference-time masks, evaluated as lenses — not training categories
- **multi-corpus stratified splits**: naive union under limit = 100% minimal_component
- **plateau cause**: MP step = Gauss-Jacobi = divergent on Ybus; sweep architecture is the
  path to machine precision (1.23e-12 shown transformer-free; xfmr null-space OPEN)

## Qualified claims (keep these honest)
- RESOLVED (T26): with --with-mesh + --no-cur, meshed/bridge-chord feeders train and
  evaluate (tree decoder skipped; Ybus physics is topology-agnostic). The radial-only
  caveat applies ONLY to runs using the tree-current decoder path. IEEE 30 Bus's
  3-DOF decoder gap likewise only affects that path.
- exclusion counts at 240f: train 2/240 (0.8%), eval 4/30 (13%) -- the vendored corpora
  are meshed-heavy relative to SMART-DS.

## Slurm strategy (fast acceptance)
- Ask the SHORTEST honest walltime: gates finish ~35 min — request 1:00:00, not 4:00:00.
  Short jobs backfill into scheduling gaps; a 4h request waits for a 4h hole.
- Probes/gates -> `gpu-h100s` (4h cap, backfills well). Smokes -> `debug-gpu`
  (fast but QOSMaxJobsPerUser=1). Only the full multi-day run -> `gpu-h100`.
- Check `sinfo -p <part> -o "%P %F"` (A/I/O/T) before choosing; 0 idle = queue anyway
  but expect backfill only if walltime is short.
